APP_NAME="Supply Chain Control Tower"
APP_VERSION="0.1.0"

# Local prototype database.
DATABASE_URL="sqlite:///data/control_tower.db"

# Future cloud PostgreSQL example.
# Keep the real password in backend/.env only. Do not paste it into chat.
# DATABASE_URL="postgresql+psycopg://USER:PASSWORD@HOST:5432/DATABASE"
